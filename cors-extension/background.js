// Background script for the CORS Fixer extension
console.log("SYNCER CORS Fixer extension loaded");

// The extension uses declarative rules defined in rules.json
// No additional background script logic is needed
